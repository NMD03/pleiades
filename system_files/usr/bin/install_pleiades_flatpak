#!/usr/bin/env bash
set -euo pipefail

flatpak remote-add --if-not-exists flathub \
    https://dl.flathub.org/repo/flathub.flatpakrepo

xargs -a /usr/share/pleiades/flatpaks \
    flatpak install -y flathub
